Судя по описанию, у вас не просто "закончилась память PHP", а идёт **утечка памяти** в одном из скриптов или библиотек, из-за чего процесс Apache с модулем PHP (mod\_php) постепенно раздувается до гигабайт, пока не упрётся в ограничения ОС или самого интерпретатора.

Давайте разберём по пунктам.

---

### 1. Почему ошибка появляется при большом `memory_limit`

* `memory_limit=1500M` в `php.ini` — это **предел для одного PHP-скрипта**, а не всего сервера.
* В Windows Server 2003 32-битный процесс физически не может адресовать больше \~2 ГБ памяти (обычно \~1.8 ГБ для пользовательского пространства).
* Когда ваш `child` Apache с mod\_php загружает всё больше данных (из-за утечки), он доходит до этого лимита, и любая новая попытка выделить память выбивает `Fatal error: Out of memory`.
* Ошибка говорит: уже выделено 786 432 байт (это не общий объём, а блок), и пытается выделить ещё 393 216 байт — но в адресном пространстве **нет подходящего непрерывного блока**.

---

### 2. Признаки утечки

Вы пишете:

> у child — 161MB и он постоянно увеличивается (доходил до 2GB)
> Это явный индикатор, что память не освобождается после обработки запросов.

На Windows с Apache 2.2 + mod\_php это часто связано с:

* **Долгоживущими объектами** или большими массивами, которые сохраняются в глобальных переменных или сессиях.
* Библиотеками с C-расширениями (GD, ImageMagick, cURL, XML-парсеры), где есть баги в освобождении ресурсов.
* Постоянным кэшированием в APC или других in-memory кэшах без лимита.
* Скриптами, которые открывают файлы/соединения и не закрывают их.
* **Известным багом в PHP 5.2–5.3 под Windows**, где `memory_get_usage()` не всегда отражает реальный рост памяти, а mod\_php "накапливает" фрагменты.

---

### 3. Что можно сделать

#### 🔹 На уровне конфигурации

* **Поменять модель работы PHP**: вместо mod\_php запустить PHP как FastCGI (php-cgi или php-fpm через mod\_fcgid).
  Тогда каждый процесс будет жить ограниченное время, и утечки не будут накапливаться.
* **Ограничить время жизни child-процессов** Apache:

  ```apache
  MaxRequestsPerChild 100
  ```

  (В Windows параметр может называться `MaxConnectionsPerChild` в mpm\_winnt).
* Снизить `memory_limit` до разумных значений (256–512M), чтобы утечка проявлялась быстрее и не забивала весь процесс.

#### 🔹 На уровне кода

* Локализовать утечку:

  * Запускать проблемный скрипт в цикле и смотреть `memory_get_usage()` на каждом шаге.
  * Проверить массивы, загружаемые в память (особенно `file_get_contents()` больших файлов).
  * Если используется GD — всегда вызывать `imagedestroy($img)`.
  * Если используются SimpleXML или DOM — после работы занулять переменные.
* Проверить, нет ли рекурсивных ссылок в объектах, мешающих сборщику мусора.

#### 🔹 На уровне ОС

* Помнить, что Windows Server 2003 32-битный по сути **всегда упирается в \~2ГБ на процесс**, так что даже без ошибок `memory_limit=1500M` очень близко к потолку.

---

💡 **Совет:**
Если оставить всё как есть (mod\_php в Apache под Windows), вы всегда будете упираться в рост памяти. Самое надёжное решение — перевести PHP в FastCGI или вообще на отдельный PHP-FPM процесс (на Linux или Windows), где процессы будут перезапускаться и утечки не будут критичны.

---

Если хотите, я могу расписать, **как быстро найти участок кода, где происходит утечка**, чтобы не просто перезапускать процессы, а реально её убрать.
Хотите, чтобы я сделал этот план поиска?
//-------------------------------------------------------------------------------------------------------------------------------------------------------------


Хорошо, тогда сделаем пошагово, прямо под ваш стек: **Windows Server 2003 + Apache 2.2.11 + PHP**.
Мы просто уберём `mod_php` и подключим PHP через FastCGI, чтобы каждый процесс PHP жил недолго и утечки не убивали Apache.

---

## 1. Убираем mod\_php

В `httpd.conf` найдите строки вида:

```apache
LoadModule php5_module "C:/php/php5apache2_2.dll"
AddType application/x-httpd-php .php
PHPIniDir "C:/php"
```

и закомментируйте их:

```apache
# LoadModule php5_module "C:/php/php5apache2_2.dll"
# AddType application/x-httpd-php .php
# PHPIniDir "C:/php"
```

Это отключит встроенный модуль PHP в Apache.

---

## 2. Ставим mod\_fcgid

* Скачайте **mod\_fcgid** для вашей версии Apache 2.2 под Windows (нужна сборка под ту же архитектуру — 32-bit или 64-bit).
  Например: [https://www.apachelounge.com/download/](https://www.apachelounge.com/download/) (в архиве ищите `mod_fcgid.so`).
* Скопируйте `mod_fcgid.so` в папку `modules` Apache.
* В `httpd.conf` добавьте:

```apache
LoadModule fcgid_module modules/mod_fcgid.so

# Каталог, где будет запускаться php-cgi.exe
FcgidInitialEnv PHPRC "C:/php"

# Настройки FastCGI
FcgidIOTimeout 120
FcgidConnectTimeout 30
MaxRequestsPerProcess 500
FcgidMaxProcesses 50
FcgidProcessLifeTime 300
FcgidMaxRequestLen 104857600

# Ассоциация PHP-скриптов с php-cgi.exe
<FilesMatch "\.php$">
    AddHandler fcgid-script .php
    FcgidWrapper "C:/php/php-cgi.exe" .php
</FilesMatch>
```

---

## 3. Настраиваем PHP-CGI

* В `C:/php` у вас уже должен быть `php-cgi.exe` (идёт в комплекте с PHP для Windows).
* Проверьте, что в `php.ini` стоит:

```ini
cgi.force_redirect = 0
fastcgi.impersonate = 1
```

* Оставьте ваш `memory_limit` (например, 512M или 1024M).

---

## 4. Перезапускаем Apache

```cmd
httpd -k restart
```

или через `services.msc`.

---

## 5. Проверяем

* Откройте `phpinfo()` в браузере.
* Убедитесь, что **Server API** теперь показывает `CGI/FastCGI`, а не `Apache 2.0 Handler`.
* Теперь, даже если скрипт будет течь по памяти, PHP-процесс умрёт после `MaxRequestsPerProcess` или `FcgidProcessLifeTime`, и Apache не будет раздуваться до 2 ГБ.

---

💡 Плюсы:

* Каждое выполнение PHP идёт в отдельном процессе, который можно убить/перезапустить.
* Утечки не копятся в основном процессе Apache.
* Можно гибко управлять числом параллельных PHP-процессов.

---

Если хотите, я могу сразу дать **готовый рабочий httpd.conf для Apache 2.2 + mod\_fcgid + PHP 5.x под Windows Server 2003**, чтобы вы просто вставили и запустили.
Хотите, чтобы я его сделал?
